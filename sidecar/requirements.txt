# The sidecar uses only the Python standard library (http.server, subprocess, struct).
# Its one real dependency is the RocksDB `ldb` CLI tool, installed via the Dockerfile (apt),
# not pip. No Python packages are required.
